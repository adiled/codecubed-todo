import React from 'react'
import styled from 'styled-components'

const Button = styled.button`
  border: 0; background: rgb(154,70,255); color: #fff; border-radius: 20px; padding: 20px; 
`

export default Button